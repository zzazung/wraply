src
├ app
│  ├ router.tsx
│  └ routes
│     ├ publicRoutes.tsx
│     ├ protectedRoutes.tsx
│     ├ projectRoutes.tsx
│     ├ buildRoutes.tsx
│     ├ dashboardRoutes.tsx
│     ├ accountRoutes.tsx
│     └ certificateRoutes.tsx
│
├ components
│  ├ ui                ← 디자인 시스템 (Button, Card 등)
│  ├ auth              ← 로그인/회원가입 UI
│  ├ build             ← 빌드 관련 UI
│  ├ artifact          ← 결과 UI
│  ├ projects          ← 프로젝트 UI
│  └ layout            ← Header, Sidebar
│
├ hooks                ← 상태 + 로직 (UI와 분리)
│  ├ useAuth.ts
│  ├ useProjects.ts
│  ├ useBuild.ts
│  ├ useArtifacts.ts
│  ├ useBuildLogs.ts
│  └ useWebSocket.ts
│
├ pages                ← 라우트 단위 화면
│  ├ auth
│  │  ├ LoginPage.tsx
│  │  └ RegisterPage.tsx
│  │
│  ├ dashboard
│  │  └ DashboardPage.tsx
│  │
│  ├ projects
│  ├ builds
│  ├ install
│  └ account
│
├ providers
│  └ WebSocketProvider.tsx   ← (선택) 전역 WS 관리
│
├ services             ← API 호출 (순수 함수)
│  ├ api.ts            ← axios 인스턴스
│  ├ auth.ts
│  ├ projects.ts
│  ├ builds.ts
│  ├ artifacts.ts
│  └ websocket.ts
│
├ stores               ← 전역 상태 (zustand)
│  ├ authStore.ts
│  └ buildStore.ts
│
├ types                ← 타입 정의
│
├ utils                ← 순수 함수
│  ├ buildStatus.ts
│  └ formatDate.ts
│
└ styles
   └ globals.css